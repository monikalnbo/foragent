[package]
name = "myagent_protocol"
version = "0.1.0"
edition = "2021"

[lib]
name = "myagent_protocol"
crate-type = ["cdylib", "rlib"]

[dependencies]
myagent_types = { path = "../types" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1"
