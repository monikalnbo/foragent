[package]
name = "myagent_setup"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "AgentOS-Setup-x64"
path = "src/main.rs"

[dependencies]
eframe = "0.28"
egui = "0.28"
zip = { version = "0.6", default-features = false, features = ["deflate"] }
